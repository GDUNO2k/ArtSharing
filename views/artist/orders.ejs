<% bodyClass = "channel" %>

<%- include("../shared/_header.ejs") %>

<%- include("./_artist_top.ejs") %>

<div class="content-wrapper">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 channels">

                <!-- Featured Videos -->
                <div class="content-block ">
                
                    <div class="channels-content">
                        <div class="clearfix"></div>
                        <div class="theme-section">
                        <div class="row">
                           <div class="cb-content py-3">
                            <h3 class="text-muted">Purchased Artworks</h3>

                            <div class="row">

                            <% for(const order of orders) { %>
                                <%- include("./_order.ejs", {order}) %>
                            <% } %>

                            <% if (orders.length == 0) { %>
                                <p class="text-center">
                                    <em class="text-danger">No data.</em>
                                </p>    
                            <% } %>
                        </div>
                    </div>
                          </div>
                        </div>
                        </div>
                    </div>
                </div>
                <!-- /Featured Videos -->

            </div>
        </div>
    </div>
</div>

<%- include("../shared/_footer.ejs") %>
